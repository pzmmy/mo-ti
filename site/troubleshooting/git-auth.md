# Git 认证

Tolaria 使用系统 Git 认证。它不直接管理提供商密码。

## 症状

- 推送失败。
- 拉取时反复要求输入凭据。
- 远程拉取在某个终端中正常，但在 Tolaria 中不行。

## 检查步骤

1. 打开一个终端。
2. `cd` 进入保险库目录。
3. 运行 `git remote -v`。
4. 运行 `git fetch`。

如果在终端中 `git fetch` 失败，请先修复系统 Git 认证。

## 常见修复方法

- 使用 GitHub CLI 登录。
- 配置 SSH 密钥。
- 更新远程 URL。
- 检查凭据助手设置。
